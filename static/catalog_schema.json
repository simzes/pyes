{
  "type": "object",
  "required": ["entries", "upload"],
  "properties": {
    "landing": {
      "type": "object",
      "required": ["title", "description"],
      "properties": {
        "title": {"type": "string"},
        "description": {"type": "string"}
      }
    },
    "upload": {
      "type": "object",
      "properties": {
        "upload": {"type": "string"}
      }
    },
    "entries": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["path", "title", "icon", "description", "binary"],
        "properties": {
          "path": {"type": "string"},
          "title": {"type": "string"},
          "icon": {"type": "string"},
          "description": {"type": "string"},
          "binary": {"type": "string"}
        }
      }
    }
  }
}
